const API_ENDPOINT = "https://localhost:44334";

export const environment = {
  production: false,
  serverUrlAdmonPagos: `${API_ENDPOINT}/AdmonPago/Api`,
  serverUrlArchivo: `${API_ENDPOINT}/AdmonPago/Api/Archivo`
};